generated: '2026-08-13'
method: derived
source: openapi/insightera-nlp-platform-openapi.yml
format: flat-json
rfc9457: false
envelope:
  shape: '{"message": "<human-readable string>"}'
  fields:
  - name: message
    type: string
    description: Prose explanation of the failure. No machine-readable code accompanies it.
note: >-
  Derived from the 4xx/5xx responses declared on all 23 operations of the published Swagger 2.0
  contract, plus two error bodies observed live on 2026-08-13. The spec declares the same five
  status codes uniformly on every operation with one-word descriptions ("Bad Request",
  "Unauthorized") and no response schema, so the shape below comes from the live service rather
  than the document. There is no published error-code registry and no `application/problem+json`
  anywhere in the contract.
problems:
- status: 400
  title: Bad Request
  declared_on_operations: 23
  spec_schema: null
  observed:
    body: '{"message":"Invalid session token"}'
    trigger: Request sent with no `token` query parameter.
  remediation: >-
    Supply a valid `token` query parameter. Note that the live service answers 400 — not the
    documented 401 — for a missing or invalid credential, so a client branching on 401 to trigger
    re-authentication will never fire.
- status: 401
  title: Unauthorized
  declared_on_operations: 23
  spec_schema: null
  observed: null
  remediation: >-
    Declared on every operation but not reproduced in live probing; authentication failures were
    observed as 400 and quota failures as 403. Treat the documented 401 as aspirational.
- status: 403
  title: Forbidden — quota exhausted
  declared_on_operations: 0
  spec_schema: null
  observed:
    body: '{"message":"This token has no quota allowed on this service"}'
    trigger: Token valid but carrying no quota for the requested service.
  remediation: >-
    Request quota for that specific service from InsightEra. This status is UNDOCUMENTED — it
    does not appear on any operation in the spec, yet it is the response a caller is most likely
    to meet first, because it is what the reference's own pre-filled demo token returns.
- status: 408
  title: Request Timeout
  declared_on_operations: 23
  spec_schema: null
  observed: null
  remediation: >-
    Retry with a smaller payload. Relevant to the batch operations (`texts`/`samples` arrays) and
    to synchronous model training (`is_sync: true`), where no maximum size or timeout budget is
    published.
- status: 500
  title: Internal Server Error
  declared_on_operations: 23
  spec_schema: null
  observed: null
  remediation: Retry with backoff; contact dev@insightera.co.th if persistent.
gaps:
- No error schema is attached to any 4xx/5xx response in the spec — only a description string.
- No stable machine-readable error code; callers must string-match on `message`.
- HTTP 403 (quota) is returned by the live service but declared on zero operations.
- Documented 401 is not what the service actually returns for a bad credential.
